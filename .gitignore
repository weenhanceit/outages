# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# If you find yourself ignoring temporary files generated by your text editor
# or operating system, you probably want to add a global ignore instead:
#   git config --global core.excludesfile '~/.gitignore_global'

# Ignore bundler config.
/.bundle

# Ignore the default SQLite database.
/db/*.sqlite3
/db/*.sqlite3-journal

# Ignore all logfiles and tempfiles.
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

/node_modules
/yarn-error.log

.vagrant

# Stop Phil from checking in temporary files.
pip*

# Don't check in the documentation. Generate it as needed.
doc/
erd.pdf

# Ignore master key for decrypting credentials and more.
/config/master.key

# For stuff that gets created if using the Dockerfile image
.bundle/
.cache/
vendor/bundle
.gem

# If you're using Docker, you might want to ignore some or all of these.
# or .local/share/pry/pry_history if you need to be more exact
.local/
.irb_history
.rdbg_history
.byebug_history
# For Debian images with Bash
.bash_history
# For Alpine images
.ash_history
.sqlite_history

docker-compose.override.yml

# Ignored for Docker and VScode dev container
.cache/
.vscode-server/
.gnupg/
# The following is here because using SSH in the container sets `known_hosts`.
.ssh/
# End of the stuff ignored for Docker
