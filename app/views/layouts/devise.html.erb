<!DOCTYPE html>
<html>
  <head>
    <%= render "head" %>
  </head>

  <body>
    <%= render "site_nav" %>
    <%= render "flash" %>

    <div class="container">
        <div class="row justify-content-center">
          <%# FIXME: Watch how the buttons change sizes non-intuitively. %>
          <div class="col-md-5">
            <%= yield %>
          </div>
        </div>
    </div>

    <%= render "footer" %>
  </body>
</html>
